<!-- <section class="why-choose-sec section">
  <div class="container position-relative">
    <h1 class="pholder text-center w-100">Why Choose Us</h1>
    <div class="row">
      <div class="col-lg-12" style="z-index: 1;">
        <h4 class="small-head text-center">Why Choose Us</h4>
        <h2 class="main-sec-head text-center">Experienced. Curious. Problem Solver</h2>
        <p class="text-paragraph text-center">
          Ask our clients—they'll tell you our team is a blast. We tell companies to choose STL not just for our
          skills and experience but for the awesome team that brings ideas to life.
        </p>


        <div class="services-carousel">
          <div class="service-card" *ngFor="let item of serviceItems">
            <img [src]="item.img" [alt]="item.alt" />
            <h3>{{ item.title }}</h3>
            <p>{{ item.description }}</p>
          </div>
        </div>

      </div>
    </div>
  </div>
</section> -->




<section class="why-choose-sec section">
  <div class="container position-relative">
    <h1 class="pholder text-center w-100">Why Choose Us</h1>
    <div class="row">
      <div class="col-lg-12" style="z-index: 1;">
        <h4 class="small-head text-center">Why Choose Us</h4>
        <h2 class="main-sec-head text-center">Experienced. Curious. Problem Solver</h2>
        <p class="text-paragraph text-center">
          Ask our clients—they'll tell you our team is a blast. We tell companies to choose STL not just for our
          skills and experience but for the awesome team that brings ideas to life.
        </p>



       <div class="services-carousel-wrapper">
  <img
    class="arrow-btn left" src="/assets/images/arrow left.png" alt="Left"
    (click)="prevSlide()" [class.disabled]="currentIndex === 0"/>

  <div class="services-carousel">
    <div
      class="service-card"
      *ngFor="let item of visibleItems"
    >
       <div class="icon-circle">
      <img [src]="item.img" [alt]="item.alt" />
    </div>
      <h3>{{ item.title }}</h3>
      <p>{{ item.description }}</p>
    </div>
  </div>

  <img
    class="arrow-btn right"
    src="/assets/images/arrow right.png"
    alt="Right"
    (click)="nextSlide()"
    [class.disabled]="currentIndex + cardsPerView >= serviceItems.length"
  />
</div>


      </div>
    </div>
  </div>
</section>
