import { ComponentFixture, TestBed } from '@angular/core/testing';

import { DriveBusinessComponent } from './drive-business.component';

describe('DriveBusinessComponent', () => {
  let component: DriveBusinessComponent;
  let fixture: ComponentFixture<DriveBusinessComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [DriveBusinessComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(DriveBusinessComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
