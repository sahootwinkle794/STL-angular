import { ComponentFixture, TestBed } from '@angular/core/testing';

import { WhatAreComponent } from './what-are.component';

describe('WhatAreComponent', () => {
  let component: WhatAreComponent;
  let fixture: ComponentFixture<WhatAreComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [WhatAreComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(WhatAreComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
