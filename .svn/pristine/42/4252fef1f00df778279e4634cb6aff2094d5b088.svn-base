import { ComponentFixture, TestBed } from '@angular/core/testing';

import { HighImpactComponent } from './high-impact.component';

describe('HighImpactComponent', () => {
  let component: HighImpactComponent;
  let fixture: ComponentFixture<HighImpactComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [HighImpactComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(HighImpactComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
